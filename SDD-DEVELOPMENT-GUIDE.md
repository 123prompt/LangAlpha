# 基于 SDD 思想的开源项目二次开发流程指南

> **SDD = Specification-Driven Development（规格驱动开发）**
> 核心思想：**先理解，再规格，后实现**。在动手改代码之前，先用规格（Specification）把"要做什么"和"改哪里"彻底想清楚，然后让 AI 工具按规格执行。

---

## 全局流程总览

```
Phase 0: 项目评估 ──→ 决定是否基于此项目二次开发
Phase 1: 项目理解 ──→ 建立心智模型
Phase 2: 需求规格 ──→ 把"我要什么"写成可执行的规格
Phase 3: 改动定位 ──→ 精确找到要改的文件和代码
Phase 4: 实现规格 ──→ 按规格写代码（AI执行）
Phase 5: 集成验证 ──→ 跑通、联调、回归
Phase 6: 交付部署 ──→ 文档、配置、上线
```

---

## Phase 0：项目评估 — 这个开源项目值得 Fork 吗？

**目标**：在投入时间之前，快速判断项目是否适合作为二次开发基础。

### 评估清单

| 维度 | 检查项 |
|------|--------|
| 活跃度 | 最近 commit 时间、issue 响应速度、PR 合并频率 |
| 许可证 | 是否允许商业使用（Apache-2.0 / MIT ✅，GPL ⚠️） |
| 架构匹配 | 技术栈是否与你的团队/能力匹配 |
| 模块化程度 | 能否只改一部分，不需要全量理解 |
| 二次开发友好度 | 有无插件机制、配置化扩展点、Hook 回调 |

### 提示词

```
我正在评估是否基于 [项目名] 进行二次开发。请帮我分析：

1. 这个项目的架构是否模块化？我能否只理解和修改其中一部分？
2. 项目有哪些扩展点（插件、Hook、配置、事件机制）可以让我不改核心代码就增加功能？
3. 项目的技术栈是否适合小团队长期维护？
4. 有哪些常见的二次开发陷阱（硬编码、紧耦合、缺少抽象层）？

项目仓库：[贴入项目根目录的文件树或 README]
```

### 以当前仓库（LangAlpha）为例

```
我正在评估是否基于 LangAlpha 进行二次开发。请帮我分析：

1. 这个项目的架构是否模块化？我能否只理解和修改其中一部分？
2. 项目有哪些扩展点可以让我不改核心代码就增加功能？
3. 项目的技术栈（Python FastAPI + React + PostgreSQL + Redis）是否适合小团队长期维护？
4. 有哪些常见的二次开发陷阱？

项目结构概览：
- src/server/  — FastAPI 后端（API路由、数据库、服务层）
- src/ptc_agent/ — AI Agent 核心（Agent图、中间件、工具、子代理）
- src/tools/  — 金融数据工具集
- skills/     — 可插拔技能模块（每个是一个 SKILL.md）
- mcp_servers/ — MCP 数据服务器
- web/        — React 前端
- migrations/ — 数据库迁移
```

---

## Phase 1：项目理解 — 建立心智模型

**目标**：不用读完所有代码，但要建立"从用户请求到代码响应"的完整链路心智模型。

### 1.1 宏观架构理解

**核心问题**：请求从哪进来？经过什么处理？最终到哪去？

#### 提示词

```
请帮我理解 [项目名] 的宏观架构，我需要建立从"用户请求"到"系统响应"的完整心智模型。

请按以下维度梳理：
1. **入口层**：用户请求从哪里进入系统？（HTTP路由 / CLI / WebSocket等）
2. **处理链路**：请求经过哪些核心模块依次处理？用 → 连接
3. **数据层**：涉及哪些存储？什么数据存哪里？
4. **外部依赖**：系统依赖哪些外部服务？各自的作用？
5. **模块边界**：各核心模块之间的接口是什么？它们如何通信？

我需要的是"端到端的链路图"，而不是逐文件的API文档。
```

#### 以 LangAlpha 为例

```
请帮我理解 LangAlpha 的宏观架构：

1. 用户在 Web UI 发起一个聊天消息，请求经过哪些模块到达 AI Agent？
2. Agent 如何决定使用哪个工具（原生工具 vs MCP服务器 vs PTC代码执行）？
3. 工具执行的结果如何回传到前端（SSE/WebSocket）？
4. Workspace 持久化机制是什么？agent.md 如何跨线程保持上下文？
5. 数据层：PostgreSQL 存什么？Redis 存什么？各自的职责边界？

我需要端到端的链路图，不是逐文件API文档。
```

### 1.2 核心模块深潜

**原则**：只深潜你**需要修改**的模块，其他模块知道边界即可。

#### 提示词

```
我需要深潜理解 [模块名] 模块，请帮我分析：

1. **职责**：这个模块做什么？不做什么？
2. **接口**：暴露了哪些函数/类/API？调用者如何使用它？
3. **依赖**：它依赖哪些其他模块？依赖方向是单向的吗？
4. **数据流**：数据从哪来？到哪去？中间经历了什么变换？
5. **配置**：有哪些可配置项？默认值是什么？改变配置的影响范围？

请结合具体代码文件说明，给出关键文件路径。
```

#### 以 LangAlpha 的 Agent 模块为例

```
我需要深潜理解 LangAlpha 的 ptc_agent 模块（src/ptc_agent/）：

1. Agent 的 graph 是如何构建的？节点和边分别是什么？
2. Middleware 栈是如何工作的？每个 middleware 的职责？
3. 工具发现机制（tool discovery）怎么实现？Agent 如何知道有哪些工具可用？
4. 子代理（subagent）的生命周期：创建 → 执行 → 结果回收 → 销毁？
5. Skills 是如何加载和激活的？SKILL.md 的格式和作用？

请结合具体代码文件说明，给出关键文件路径。
```

### 1.3 数据模型理解

**核心问题**：系统中有哪些核心实体？它们之间什么关系？

#### 提示词

```
请帮我梳理 [项目名] 的核心数据模型：

1. 有哪些核心实体（用户、工作区、会话等）？
2. 实体之间的关系是什么？（一对多、多对多等）
3. 每个实体的关键字段和生命周期？
4. 数据库迁移（migration）的演进历史说明了什么业务变化？

请从 migration 文件和 model 定义中提取信息。
```

#### 以 LangAlpha 为例

```
请帮我梳理 LangAlpha 的核心数据模型：

1. 从 migrations/versions/ 和 src/server/models/ 中提取核心实体
2. User / Workspace / Thread / Turn / Automation 等实体之间的关系
3. 每个实体的生命周期（创建→更新→删除的触发条件）
4. migration 演进说明了什么业务变化（如 012_add_mcp_servers 说明后来才支持MCP）
```

### 1.4 前端架构理解（如需改前端）

#### 提示词

```
请帮我理解 [项目名] 的前端架构：

1. 路由结构：有哪些页面？URL 映射关系？
2. 状态管理：用了什么方案？全局状态 vs 局部状态的划分？
3. 组件层次：核心组件的嵌套关系和数据流方向？
4. API 通信：如何与后端通信？SSE/WebSocket 的使用场景？
5. 样式方案：CSS 框架、主题系统、响应式策略？
```

---

## Phase 2：需求规格 — 把"我要什么"写成可执行的规格

**目标**：把模糊的"我想加个功能"转化为精确的、AI可执行的规格文档。

### SDD 规格模板

每个需求写一份规格，格式如下：

```markdown
## SPEC: [需求名称]

### 背景
为什么需要这个功能？解决什么问题？

### 用户故事
作为 [角色]，我希望 [动作]，以便 [价值]。

### 功能规格
1. [具体行为描述，用 Given-When-Then 格式]
2. ...

### 非功能规格
- 性能要求：...
- 安全要求：...
- 兼容性要求：...

### 改动范围预估
- 后端：[哪些模块/文件]
- 前端：[哪些页面/组件]
- 数据库：[新增/修改的表]
- 配置：[新增的配置项]

### 不做的事情（边界）
- 本规格 **不包含** ...

### 验收标准
- [ ] 可验证的条件1
- [ ] 可验证的条件2
```

### 提示词：需求 → 规格

```
我有一个二次开发需求，请帮我把需求转化为 SDD 规格文档。

## 我的需求
[用自然语言描述你想要什么，越详细越好]

## 当前项目上下文
- 项目名：[项目名]
- 技术栈：[后端/前端/数据库]
- 核心架构：[一句话描述]

请按以下格式输出规格：

1. **背景**：为什么需要这个功能
2. **用户故事**：As a... I want... So that...
3. **功能规格**：用 Given-When-Then 格式描述每个行为
4. **改动范围预估**：需要改哪些模块/文件
5. **不做的事**：明确边界，防止范围蔓延
6. **验收标准**：可验证的、可打勾的条件

要求：规格要精确到"一个程序员拿到就能直接实现"的程度，不留歧义。
```

### 以 LangAlpha 为例：添加"投资组合风险评估"功能

```
我有一个二次开发需求，请帮我把需求转化为 SDD 规格文档。

## 我的需求
我想在 LangAlpha 中添加"投资组合风险评估"功能。用户在 Portfolio 中添加持仓后，
系统能自动计算组合的风险指标（波动率、最大回撤、夏普比率、相关性矩阵），
并在前端展示风险仪表盘。

## 当前项目上下文
- 项目名：LangAlpha
- 技术栈：Python FastAPI + React + PostgreSQL + Redis
- 核心架构：Agent 通过 PTC 在沙箱中执行代码，调用 MCP 服务器获取金融数据
- 已有模块：Portfolio（CRUD）、Market Data（价格数据）、Workspace（持久化）

请按 SDD 格式输出规格，精确到"一个程序员拿到就能直接实现"的程度。
```

### 提示词：规格审查

```
请审查以下 SDD 规格文档，检查是否存在：

1. **歧义**：是否有多种理解方式的描述？
2. **遗漏**：是否遗漏了边界条件、错误处理、并发场景？
3. **不可验证**：验收标准是否都是可客观判断的？
4. **范围蔓延**：是否有"顺便也做一下"的隐含需求？
5. **架构冲突**：是否与现有架构有矛盾？需要改核心模块吗？

规格文档如下：
[贴入规格]
```

---

## Phase 3：改动定位 — 精确找到要改什么

**目标**：在动手之前，精确定位需要新增/修改/删除的文件和代码段。

### 3.1 影响面分析

#### 提示词

```
基于以下 SDD 规格，请帮我做改动定位分析：

规格摘要：[贴入规格的功能规格部分]

项目结构：
[贴入项目文件树，至少到第3层目录]

请输出：
1. **需要新增的文件**：路径 + 职责
2. **需要修改的文件**：路径 + 修改什么 + 为什么
3. **需要删除的文件**：路径 + 原因
4. **改动依赖链**：哪些改动必须先做？哪些可以并行？
5. **风险点**：改动可能破坏什么？需要回归测试什么？

注意：只改必要的，不要重构不在规格范围内的代码。
```

#### 以 LangAlpha 为例

```
基于"投资组合风险评估"的 SDD 规格，请帮我做改动定位分析：

规格摘要：
- 在 Portfolio 持仓基础上计算风险指标（波动率、最大回撤、夏普比率、相关性矩阵）
- 后端新增 /portfolio/risk-analysis API
- 新增 MCP 工具供 PTC Agent 调用
- 前端新增风险仪表盘页面

项目结构：
src/server/app/portfolio.py  — Portfolio API 路由
src/server/database/portfolio.py  — Portfolio 数据库操作
src/server/models/workspace.py  — 数据模型
src/tools/market_data/  — 市场数据工具
mcp_servers/  — MCP 服务器目录
web/src/  — React 前端

请输出改动定位分析。
```

### 3.2 代码定位深潜

当影响面分析确定了具体文件后，需要深入理解现有代码。

#### 提示词

```
我需要修改 [文件路径] 中的 [函数名/类名]，请帮我：

1. 读取这个文件，解释 [函数名/类名] 的完整逻辑
2. 列出所有调用它的地方（上游调用者）
3. 列出它调用的所有下游依赖
4. 指出修改时需要注意的约束（参数格式、返回值约定、副作用等）
5. 建议最安全的修改方式（新增 vs 修改 vs 包装）

我需要的不是代码重构建议，而是"最小改动路径"。
```

---

## Phase 4：实现规格 — AI 按规格执行

**目标**：用 AI 工具按照规格逐个实现改动点。

### 4.1 实现前准备：上下文注入

**关键原则**：AI 的输出质量 = 你给的上下文质量。每次让 AI 写代码前，先注入足够的上下文。

#### 提示词模板

```
你是一个高级开发者，正在对 [项目名] 进行二次开发。

## 项目上下文
- 技术栈：[后端/前端/数据库]
- 架构模式：[MVC / Clean Architecture / ...]
- 代码规范：[linting规则、命名约定、文件组织方式]

## 当前任务
根据以下 SDD 规格，实现 [具体改动点]：

规格：[贴入相关规格片段]

## 现有代码参考
[贴入需要修改的文件的关键代码段]

## 约束
1. 遵循项目现有的代码风格和模式
2. 不要修改规格范围外的代码
3. 如果发现规格有问题，先指出再实现
4. 新增代码要与现有代码的抽象层级一致

## 输出要求
1. 给出完整的修改后代码（不要省略，不要用 ... 代替）
2. 说明修改的理由
3. 列出需要注意的边界条件
```

### 4.2 逐模块实现

**原则**：按改动依赖链的顺序，一个模块一个模块地实现，每实现一个就验证一个。

#### 后端 API 新增的提示词

```
我需要在 [项目名] 中新增一个 API 端点。

## 规格要求
- 路径：[如 POST /api/portfolio/risk-analysis]
- 请求参数：[参数名、类型、必填/可选]
- 响应格式：[字段名、类型、含义]
- 业务逻辑：[步骤1 → 步骤2 → 步骤3]
- 错误处理：[各种错误场景和对应的状态码]

## 参考现有代码
[贴入项目中同类型的 API 实现作为参考，让 AI 模仿风格]

## 约束
- 使用项目现有的依赖注入、认证、错误处理模式
- 数据库操作使用项目现有的 ORM/查询方式
- 不要引入新的依赖，除非规格明确要求

请输出完整的实现代码。
```

#### 前端组件新增的提示词

```
我需要在 [项目名] 前端新增一个页面/组件。

## 规格要求
- 页面/组件名：[名称]
- 功能：[具体描述]
- 数据来源：[调哪个 API / 用哪个 hook]
- 交互行为：[用户操作 → 系统响应]
- 样式要求：[与哪个现有页面风格一致]

## 参考现有代码
[贴入项目中同类型的页面/组件实现作为参考]

## 约束
- 使用项目现有的 UI 组件库和样式系统
- 状态管理使用项目现有的方案
- API 调用使用项目现有的 client 封装

请输出完整的实现代码。
```

#### 数据库迁移的提示词

```
我需要为 [项目名] 新增一个数据库迁移。

## 变更内容
- 新增表：[表名、字段、索引]
- 修改表：[表名、新增/修改/删除的字段]
- 数据迁移：[需要回填的数据]

## 参考现有迁移
[贴入最近的 migration 文件作为参考]

## 约束
- 遵循项目现有的 Alembic 迁移命名和编号规范
- 迁移必须是可逆的（提供 downgrade）
- 不要修改已有迁移文件

请输出完整的迁移文件。
```

### 4.3 实现过程中的自检提示词

每完成一个模块的实现后，用以下提示词自检：

```
请审查以下代码变更，检查是否存在问题：

## 变更内容
[贴入代码]

## 规格要求
[贴入规格片段]

## 审查维度
1. **功能正确性**：是否完全实现了规格要求？
2. **边界条件**：空值、极端值、并发场景是否处理？
3. **安全检查**：SQL注入、XSS、认证授权是否遗漏？
4. **性能风险**：N+1查询、内存泄漏、无限循环？
5. **风格一致**：是否与项目现有代码风格一致？
6. **遗漏检查**：规格中有什么要求没有实现？

只报告问题，不要做不必要的"改进"建议。
```

---

## Phase 5：集成验证 — 跑通、联调、回归

**目标**：确保改动集成后系统整体工作正常。

### 5.1 编写测试的提示词

```
请为以下代码编写测试：

## 被测代码
[贴入代码]

## 规格要求
[贴入规格的功能规格部分]

## 测试要求
1. 遵循项目现有的测试框架和目录结构
2. 覆盖正常路径 + 每个边界条件 + 每个错误路径
3. 使用项目现有的 fixture 和 mock 方式
4. 不 mock 被测代码的内部实现，只 mock 外部依赖

## 参考现有测试
[贴入项目中同类型的测试文件作为参考]

请输出完整的测试代码。
```

### 5.2 集成联调的提示词

```
我刚完成了以下改动，请帮我做集成检查：

## 改动清单
1. [文件A] — 新增了 [功能]
2. [文件B] — 修改了 [函数]
3. [文件C] — 新增了 [路由]

## 检查维度
1. **接口一致性**：前后端接口是否对齐（请求/响应格式、状态码）？
2. **数据一致性**：新增的数据是否能被下游正确消费？
3. **配置完整性**：新增的配置项是否在所有环境都有默认值？
4. **迁移顺序**：数据库迁移是否在代码部署前执行？
5. **依赖完整**：新增的 Python/JS 依赖是否都声明了？

请逐项检查并报告问题。
```

### 5.3 回归测试的提示词

```
我的改动可能影响了以下现有功能，请帮我评估回归风险：

## 改动描述
[描述你改了什么]

## 可能受影响的现有功能
1. [功能A] — 因为 [改动点] 可能影响 [原因]
2. [功能B] — 因为 ...

请帮我：
1. 列出需要回归测试的场景（优先级排序）
2. 每个场景的验证步骤
3. 最可能的回归风险点
```

---

## Phase 6：交付部署

**目标**：让改动安全上线。

### 6.1 配置检查的提示词

```
请帮我检查以下改动涉及的配置项是否完整：

## 新增配置项
- [配置项1]：[用途] [默认值] [是否必须]
- [配置项2]：...

## 检查清单
1. 是否更新了 .env.example？
2. 是否更新了 config.yaml / agent_config.yaml？
3. 是否更新了 docker-compose.yml？
4. 是否更新了文档中关于配置的说明？
5. 新增配置是否有合理的默认值，不会导致启动失败？
```

### 6.2 部署前检查的提示词

```
请帮我做部署前的最终检查：

## 改动清单
[列出所有改动]

## 检查维度
1. **数据库迁移**：是否可逆？是否会影响线上数据？
2. **向后兼容**：API 变更是否兼容旧版客户端？
3. **资源需求**：是否新增了对外部服务的依赖？
4. **监控告警**：是否需要新增监控指标？
5. **回滚方案**：如果部署后出问题，如何回滚？

请逐项检查并给出结论。
```

---

## 附录 A：通用技巧

### A.1 让 AI 快速理解项目上下文

每次开启新对话时，先用以下提示词注入上下文：

```
这是我的项目上下文，请在后续对话中参考：

## 项目概况
- 项目名：[名称]
- 一句话描述：[做什么的]
- 技术栈：[后端/前端/数据库/基础设施]

## 核心架构
[从 Phase 1 得出的心智模型，用几行文字描述]

## 代码规范
- 命名：[snake_case / camelCase]
- 文件组织：[按功能 / 按层级]
- 测试：[框架 + 目录结构]

## 关键文件路径
- 入口：[路径]
- 配置：[路径]
- 路由：[路径]
- 数据模型：[路径]
- 测试：[路径]
```

### A.2 处理"AI 幻觉"的策略

AI 可能编造不存在的 API 或函数。防御方法：

```
在给出代码建议之前，请先验证：
1. 你引用的函数/类是否确实存在于项目代码中？
2. 你使用的方法签名是否与源码一致？
3. 如果不确定，请明确标注 [需要验证] 而不是编造。

项目代码供你参考：
[贴入相关源码]
```

### A.3 渐进式理解 — 不要一次性读完所有代码

```
我不需要理解整个项目，我只需要理解 [特定模块/功能] 的实现。

请按以下优先级给我信息：
1. 这个功能的入口点（路由/函数签名）
2. 核心处理逻辑（主流程，跳过错误处理和边界检查）
3. 关键的数据结构和返回格式

先给我骨架，我有具体问题再深问。
```

---

## 附录 B：各阶段提示词速查表

| 阶段 | 核心提示词（一句话版） |
|------|----------------------|
| Phase 0 | "这个项目值得 Fork 吗？分析模块化程度、扩展点、技术栈匹配度、二次开发陷阱" |
| Phase 1.1 | "从用户请求到系统响应，完整的处理链路是什么？" |
| Phase 1.2 | "我需要改 [模块]，它的职责/接口/依赖/数据流是什么？" |
| Phase 1.3 | "核心数据实体有哪些？它们之间的关系是什么？" |
| Phase 1.4 | "前端的路由/状态管理/组件层次/API通信是怎样的？" |
| Phase 2 | "把我的需求转化为 SDD 规格：背景、用户故事、功能规格、改动范围、不做的事、验收标准" |
| Phase 3 | "基于规格，列出需要新增/修改/删除的文件，以及改动依赖链和风险点" |
| Phase 4.1 | "注入上下文 + 规格 + 现有代码参考 + 约束 → 输出完整实现代码" |
| Phase 4.3 | "审查代码：功能正确性、边界条件、安全、性能、风格、遗漏" |
| Phase 5.1 | "为这段代码写测试：正常路径 + 边界 + 错误路径" |
| Phase 5.2 | "集成检查：接口一致、数据一致、配置完整、迁移顺序、依赖完整" |
| Phase 5.3 | "回归风险：哪些现有功能可能受影响？验证步骤？" |
| Phase 6 | "部署前检查：迁移可逆、向后兼容、资源需求、回滚方案" |

---

## 附录 C：以 LangAlpha 为例的完整演练

以下是以本仓库（LangAlpha）为例，从零开始进行二次开发的完整提示词序列。

### Step 1: 项目评估

```
我正在评估是否基于 LangAlpha 进行二次开发。

项目概况：LangAlpha 是一个 AI 投资助手平台，技术栈为 Python FastAPI + React 19 + PostgreSQL + Redis + LangGraph。

请帮我分析：
1. 架构模块化程度：能否只改其中一个模块（如只改 Skills、只改前端、只改 MCP 服务器）？
2. 扩展点：项目有哪些可以不改核心代码就扩展的机制？
3. 技术栈维护难度：对3人小团队是否可行？
4. 二次开发陷阱：有哪些需要注意的问题？

项目文件树：
src/server/app/ — FastAPI 路由（workspaces, portfolio, market_data, threads 等）
src/ptc_agent/ — Agent 核心（graph, middleware, tools, subagents, prompts）
src/tools/ — 金融数据工具
skills/ — 23个可插拔技能，每个有 SKILL.md
mcp_servers/ — MCP 数据服务器
web/src/ — React 前端
```

### Step 2: 宏观架构理解

```
请帮我理解 LangAlpha 的宏观架构，我需要端到端的链路图：

1. 用户在 Web UI 发起聊天 → 请求路径：web/src/api/client.ts → src/server/app/threads.py → handler → agent graph → tools → 响应回前端
2. SSE 流式响应的机制是什么？
3. PTC（Programmatic Tool Calling）的执行流：Agent 写 Python → 沙箱执行 → MCP 服务器 → 金融API → 结果回传
4. Workspace 持久化：agent.md 如何跨线程注入上下文？

请结合代码文件说明关键路径。
```

### Step 3: 深潜目标模块

```
我计划在 LangAlpha 的 Skills 体系中添加新技能。请帮我深潜 skills/ 模块：

1. SKILL.md 的格式规范是什么？（参考现有 skill 的 SKILL.md）
2. Skill 如何被 Agent 发现和激活？（查看 src/ptc_agent/ 中的 skill 加载逻辑）
3. Skill 可以引用哪些资源（scripts/, references/, assets/）？
4. Skill 与 MCP 服务器的关系是什么？

请结合 skills/目录下的具体 SKILL.md 文件和加载代码说明。
```

### Step 4: 需求规格化

```
我的需求：在 LangAlpha 中添加"投资组合风险评估"技能。

用户场景：
1. 用户在 Portfolio 中添加持仓（已有功能）
2. 用户输入 /risk-analysis 或 Agent 自动检测到持仓变化
3. Skill 激活，Agent 通过 PTC 在沙箱中获取持仓的价格历史
4. 计算风险指标：年化波动率、最大回撤、夏普比率、持仓相关性矩阵
5. 生成风险仪表盘（HTML dashboard），展示在 Workspace 中
6. 将分析结果保存到 workspace 的 results/ 目录

请转化为 SDD 规格：背景、用户故事、功能规格（Given-When-Then）、改动范围、不做的事、验收标准。
```

### Step 5: 改动定位

```
基于"投资组合风险评估"的 SDD 规格，请做改动定位：

需要新增的文件：
1. skills/risk-analysis/SKILL.md — 技能定义
2. skills/risk-analysis/references/risk-metrics.md — 风险指标计算公式参考
3. skills/risk-analysis/scripts/calculate_risk.py — PTC 沙箱中执行的计算脚本

需要修改的文件：
1. agent_config.yaml — 注册新 skill（如果需要）

不需要修改核心 Agent 代码，因为 Skill 体系是可插拔的。

请验证这个改动定位是否正确，是否有遗漏。
```

### Step 6: 逐模块实现

```
你是一个高级开发者，正在对 LangAlpha 进行二次开发。

## 项目上下文
- 技术栈：Python FastAPI + LangGraph + MCP
- Skill 体系：每个 Skill 是一个目录，核心是 SKILL.md，可包含 scripts/ 和 references/
- Agent 通过读取 SKILL.md 来理解技能，通过 PTC 执行 scripts/ 中的 Python 代码

## 当前任务
实现 risk-analysis Skill 的 SKILL.md：

规格要求：
- 技能名称：risk-analysis
- 触发条件：用户提到"风险分析"/"风险评估"/"risk analysis"，或在有持仓的 workspace 中
- 执行步骤：
  1. 从 Portfolio 获取用户持仓
  2. 通过 MCP 价格数据服务器获取每个持仓的历史价格
  3. 计算风险指标（年化波动率、最大回撤、夏普比率、相关性矩阵）
  4. 生成交互式 HTML 仪表盘
  5. 保存到 workspace 的 results/ 目录

## 参考现有 Skill
[贴入 skills/earnings-analysis/SKILL.md 的内容作为风格参考]

请输出完整的 SKILL.md 内容。
```

### Step 7: 自检

```
请审查以下 SKILL.md 实现：

[贴入生成的 SKILL.md]

审查维度：
1. 是否完全实现了规格要求？
2. Agent 能否仅凭 SKILL.md 就正确执行这个技能？
3. 风险指标的计算公式是否正确？
4. 与现有 Skill 的格式是否一致？
5. 是否遗漏了错误处理（如持仓为空、数据不足）？

只报告问题，不做不必要的改进。
```

### Step 8: 测试验证

```
请为 risk-analysis Skill 编写验证步骤：

1. 手动验证：如何在开发环境中测试这个 Skill？
2. 集成验证：如何确认 Skill 被正确发现和激活？
3. 边界测试：持仓为空、只有一个持仓、数据不足等场景如何处理？

请给出具体的命令和操作步骤。
```

---

## 附录 D：方法论核心原则

1. **理解先于行动** — 没建立心智模型之前不动手
2. **规格先于代码** — 没写清楚"做什么"之前不写"怎么做"
3. **定位先于修改** — 没找到精确改动点之前不全局搜索替换
4. **最小改动原则** — 只改规格范围内的，不顺手重构
5. **渐进式推进** — 一个模块一个模块地实现和验证
6. **AI 是执行者，你是决策者** — 你定规格，AI 写代码，你审查
7. **上下文决定质量** — 给 AI 的上下文越精确，输出质量越高
8. **每次对话重新注入上下文** — AI 无状态，每次新对话都要给足上下文
